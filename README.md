# studygit

## working with branch
* git fetch
* git checkout -t -b new_branch_name_to_create origin/remote_branch_name

```
git fetch
git checkout -t -b new_branch_name_to_create origin/remote_branch_name
```


